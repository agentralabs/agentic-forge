[package]
name = "agentic-forge-wasm"
version = "0.1.0"
edition = "2021"
description = "WASM surface for AgenticForge"
license = "MIT"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2"
